import {View, Text} from 'react-native';
import React from 'react';

const VideoPlayer = () => {
  return (
    <View>
      <VideoPlayer source={{uri: {trailers}}} />;
    </View>
  );
};

export default VideoPlayer;
